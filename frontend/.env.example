# Frontend environment variables
# Copy to .env.local for local development

# For local dev — uses Vite proxy so /api goes to localhost:8000
VITE_API_URL=/api

# For production (Vercel) — point directly to your Render backend
# VITE_API_URL=https://your-render-backend.onrender.com/api
